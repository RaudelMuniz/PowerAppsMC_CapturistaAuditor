App As appinfo:
    BackEnabled: =false
    OnStart: =Set(varCarga,false);Set(varTimer,false)
    StartScreen: =Inicio
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

