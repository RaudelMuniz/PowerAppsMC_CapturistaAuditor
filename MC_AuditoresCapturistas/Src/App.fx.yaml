App As appinfo:
    BackEnabled: =false
    OnStart: =Set(varCarga,false);Set(varTimer,false);Set(varCarga2,false);Set(varTimer2,false)
    StartScreen: =Inicio
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

